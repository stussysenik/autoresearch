{
  "name": "ocw-video-scraper",
  "version": "1.0.0",
  "type": "module",
  "description": "Benchmark 4 scraping strategies for MIT OCW video catalog extraction",
  "scripts": {
    "fetch": "bun run src/fetch_data.ts",
    "run": "bun run src/run_experiment.ts",
    "analyze": "bun run src/analyze_results.ts",
    "experiment": "bun run fetch && bun run run && bun run analyze"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
